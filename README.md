# IcoteraRecruitmentDev

Install and Run:

`make` - for compile app and lib

`make app` - to compile application only

`make lib` - to compile lib only


`make run` - to run the target

`make clean` - to clean the target and lib shared object 
 
